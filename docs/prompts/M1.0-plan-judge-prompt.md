# M1.0 — Independent Plan Judge Prompt

Status: EXECUTED — first judge returned FAIL; re-judge not authorized

Review only. Do not edit files, Git state, dependencies, external tools, or the worktree.

## Inputs

- frozen M0.0 product/architecture/roadmap package;
- frozen M0.1 implementation/evidence baseline and freeze report;
- `docs/specs/M1.0-repository-audit.md`;
- frozen `docs/specs/M1.0-transcript-selected-cut-spec.md`;
- proposed `docs/specs/M1.0-implementation-plan.md`;
- fixture policy, repository guard, manifests, lockfile, source tree, tests, and CI workflow.

## Required independent checks

Return `PASS`, `PASS WITH NOTES`, or `FAIL` with a criterion table and exact evidence. Verify:

1. M1.0 remains ingest/validation only and excludes M1.1–M1.3;
2. every frozen specification requirement maps to a checkpoint and test;
3. the transcript contract, units, limits, ordering, overlap, IDs, and duration rules are complete;
4. media stream/duration/rational/warning rules are deterministic;
5. external `ffprobe` is neither bundled nor silently installed;
6. utility-process and subprocess ownership follows the frozen architecture;
7. executable, args, environment, paths, stderr, and process handles stay privileged;
8. no shell or generic command/filesystem/raw IPC surface exists;
9. cancellation, timeout, output caps, crashes, races, and shutdown are testable;
10. native dialogs and transcript reads are bounded and main-owned;
11. renderer polling and state transitions are deterministic and accessible;
12. pure package edges and the evolved guard remain strict;
13. no registry dependency, binary fixture, LFS, CI media download, AI, database, packaging, timeline, or export scope is added;
14. tests precede implementation and cover negative/security cases;
15. checkpoints are small, independently reviewable, committable, and reversible;
16. clean-room and hosted-CI evidence are reproducible;
17. real-input smoke is correctly gated behind local prerequisite/privacy approval;
18. the plan stops before implementation and M1.1.

Inspect actual repository constraints rather than trusting the audit or plan. Surface contradictions, missing ownership, impossible tests, hidden dependency needs, platform assumptions, and scope creep.

## Output

- verdict;
- 18-row evidence table;
- blocking findings;
- non-blocking notes;
- scope-creep findings;
- required corrections;
- readiness for Checkpoint 1 authorization.

`NO REPOSITORY MODIFICATION IS AUTHORIZED BY THIS PROMPT`
