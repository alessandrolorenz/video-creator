# M0.1 Planning Prompt for Claude Code / Codex

You are working on **M0.1 — Repository Foundation** for the AI Video Assembly project.

## Authority and stopping rule

This task is **planning only**. Do not create or modify production source files. Do not install dependencies. You may create the repository audit, implementation plan, and empty implementation-report scaffold under `docs/specs/` if the repository already exists and the working tree policy permits it.

Stop after presenting the plan and evidence. Implementation requires explicit approval.

## Read first

Read and treat as source of truth:

- `README.md`
- `docs/00-product-vision.md`
- `docs/01-product-principles.md`
- `docs/02-scope-and-non-goals.md`
- `docs/03-architecture-decision.md`
- `docs/04-domain-model.md`
- `docs/05-ai-analysis-and-editing-contract.md`
- `docs/06-roadmap.md`
- `docs/07-sdd-workflow-and-gates.md`
- `docs/08-validation-and-judge-strategy.md`
- `docs/10-repository-structure.md`
- `docs/decisions/ADR-001-desktop-first-electron.md`
- `docs/decisions/ADR-003-ai-as-planner-not-executor.md`
- `docs/specs/M0.1-repository-foundation-spec.md`

## Step 1 — Audit

Report:

1. repository path;
2. branch and HEAD;
3. working-tree state;
4. Node and package-manager versions;
5. existing files and architecture;
6. existing scripts, tests, lint, typecheck, and CI;
7. existing Electron/React/TypeScript decisions, if any;
8. constraints or conflicts with the frozen M0.1 spec;
9. files that must be preserved;
10. whether the repository is empty, existing, or mixed-purpose.

Do not hide unrelated changes. Do not reset or delete them.

## Step 2 — Implementation plan

Produce a checkpointed plan that maps every M0.1 requirement to:

- exact files/directories;
- dependencies and why each is needed;
- tests written before or alongside implementation;
- security configuration;
- build configuration;
- CI commands;
- rollback boundary;
- verification evidence.

The plan must keep the renderer free from Node and Electron internals, use a narrow typed preload bridge, and avoid all product functionality forbidden by the spec.

## Step 3 — Required artifacts

Create or update only:

- `docs/specs/M0.1-repository-audit.md`
- `docs/specs/M0.1-implementation-plan.md`
- `docs/specs/M0.1-implementation-report.md` as a scaffold with PENDING fields

## Final response

Return:

- branch / HEAD;
- worktree before and after;
- baseline commands and results;
- concise architecture summary;
- checkpoint plan;
- dependency list;
- test plan;
- risks/open questions;
- exact files created or modified;
- explicit statement: `STOPPED AT M0.1 PLANNING GATE — IMPLEMENTATION NOT STARTED`.
