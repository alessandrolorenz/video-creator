# M1.0 — Independent Implementation Judge Prompt (diff-scoped)

Status: EXECUTED — prior verdict `FAIL`; re-judge `PASS WITH NOTES` on `f044b8ddd7b52768086935601c3c40517b906d1a`

Read-only review. Do not modify the repository or Git state, and do not repair the candidate.

Follow the diff-scoped protocol in [`../08-validation-and-judge-strategy.md`](../08-validation-and-judge-strategy.md):

1. Confirm hosted CI is **green on the exact candidate SHA**. Trust that run for the clean-install and full-suite reproduction — do not repeat them.
2. Review the **checkpoint diff** plus the spec section(s) it implements, against the Guarded checklist for the surfaces it touches: scope, transcript/media contracts, process/worker behaviour (fixed args, `shell:false`, cancellation/timeout/limit, child `close`), native dialogs, path redaction, strict IPC/preload, renderer sanitization/UX, architecture guards, fixtures, dependencies, and tracked/ignored state.
3. Verify no product code silently installs, discovers, or bundles a real `ffprobe`, and that the implementation is safely smokeable — without importing any private user data. Real `ffprobe` media smoke belongs to the separately authorized manual gate.

Return `PASS`, `PASS WITH NOTES`, or `FAIL`, with each finding tied to a specific file/line and spec requirement, plus readiness for manual smoke/freeze. On a re-judge, review only the correction diff plus the prior findings.
