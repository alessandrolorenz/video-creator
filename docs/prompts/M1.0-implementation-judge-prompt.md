# M1.0 — Independent Implementation Judge Prompt

Status: PRIOR EXECUTION `FAIL` on `a67538edd9f1df91e4790e6795c1b16ca6e3ce2f`; re-judge authorized only after the current follow-up reaches green hosted CI

Review the exact published implementation candidate read-only. Do not modify the repository or Git state.

Independently reproduce a clean archive install and the full verification sequence. Audit scope, transcript/media contracts, process/worker behavior, fixed arguments, cancellation/races/limits, native dialogs, path redaction, strict IPC/preload, renderer state/UX, architecture guards, fixtures, dependencies, build artifacts, hosted CI, and tracked/ignored state.

Run a real Electron inspection without importing private user data. A real `ffprobe` media smoke belongs to the separately authorized manual gate, but the judge must prove that the implementation is safely smokeable and that no product code silently installs or bundles the tool.

Return `PASS`, `PASS WITH NOTES`, or `FAIL`, with evidence, blocking findings, failures/retries, and readiness for manual smoke/freeze.
