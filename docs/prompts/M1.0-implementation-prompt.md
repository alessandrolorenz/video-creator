# M1.0 — Checkpoint Implementation Prompt

Status: HISTORICAL CHECKPOINT TEMPLATE — all M1.0 checkpoints were later authorized; active follow-up state is in `docs/PROJECT-STATE.md`

Implement only the exact checkpoint explicitly authorized by the product owner from `docs/specs/M1.0-implementation-plan.md`.

Before changing anything:

1. confirm the frozen spec SHA and approved plan verdict;
2. confirm branch, HEAD, tracked/ignored worktree state, toolchain, and checkpoint authorization;
3. read the complete checkpoint scope, tests-first order, stop, and rollback;
4. reject any requested work that belongs to another checkpoint or M1.1+.

During the checkpoint:

- write failing tests/guards first where technically possible;
- use only planned files and dependencies;
- never install `ffprobe`, add binary media, weaken the renderer boundary, expose paths, add a shell, or introduce a generic privileged bridge;
- preserve user changes and frozen evidence;
- update `M1.0-implementation-report.md` with exact commands, results, failures/retries, files, security evidence, and deviations;
- run frozen install, targeted checks, and the complete repository sequence;
- inspect generated/ignored state;
- stage only checkpoint files and create one local commit when authorized;
- do not push;
- stop before the next checkpoint.

If the frozen spec and approved plan cannot be satisfied, stop and report the blocking conflict rather than improvising a broader design.
