# M0.1 Independent Implementation Judge Prompt

Act as an independent implementation judge. Inspect actual source, configuration, tests, diffs, frozen spec, approved plan, and implementation report. Do not modify files and do not trust the report without reproducing evidence.

Verify:

1. Scope is limited to M0.1.
2. Electron main/preload/renderer boundaries are real.
3. `nodeIntegration` is disabled.
4. context isolation is enabled.
5. renderer sandbox policy matches the spec.
6. preload exposes only narrow typed methods.
7. IPC inputs are runtime validated.
8. no generic command execution or unrestricted filesystem API exists.
9. domain packages do not import Electron or application code.
10. media and AI packages contain contracts only.
11. branded time/range invariants have meaningful tests.
12. package-boundary guards exist and pass.
13. lint, typecheck, tests, and all builds pass from a clean install.
14. CI runs the required checks.
15. worktree and generated-file state are understood.
16. documentation accurately reflects actual implementation.

Return a detailed evidence table and verdict: PASS / PASS WITH NOTES / FAIL. State whether M0.1 is ready for manual smoke and freeze.
