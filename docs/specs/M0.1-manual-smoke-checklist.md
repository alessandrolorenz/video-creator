# M0.1 — Manual Smoke Checklist

Status: COMPLETE — PASS

M0.1 has no editing workflow. The smoke validates the desktop shell and security-visible behavior only.

- [x] App launches from a clean install.
- [x] Main window renders the neutral repository-foundation screen.
- [x] Reload does not expose Node globals to renderer code.
- [x] DevTools inspection confirms no unrestricted privileged API on `window`.
- [x] Closing the window terminates background workers cleanly.
- [x] No media, transcript, AI, persistence, or export UI is present.
- [x] Packaged development build, when applicable, starts without a blank screen. Packaging is not part of M0.1; the applicable compiled development output started and rendered correctly.

Result: **PASS**

## Execution record

- Date: 2026-07-22 (America/Sao_Paulo)
- Reviewed branch / commit: `main` / `09be5ceb8b7efa4e4fc04d573a71f0c5fff606ca`
- Authoritative clean-room: `/private/tmp/ai-video-editor-M0.1-manual-smoke-authoritative.DuKTCl`
- Initial state: exact 100-file `git archive`; no Git metadata or dependency tree
- Environment: macOS arm64, Node `24.18.0`, pnpm `11.9.0`, Electron `43.2.0`
- Installation: `pnpm install --frozen-lockfile` — PASS; 189 locked packages, resolution skipped, `0.62s`
- Build: `pnpm build` — PASS; compiler `6.0.3`, all participating packages and desktop targets built

## Visual and runtime evidence

- Electron loaded `file:///private/tmp/ai-video-editor-M0.1-manual-smoke-authoritative.DuKTCl/apps/desktop/dist/renderer/index.html`.
- The captured 1920×1216 renderer showed only `AI VIDEO ASSEMBLY`, `Repository foundation`, `Secure desktop foundation ready.`, and the M0.1 boundary note.
- Screenshot SHA-256: `e755eeb40e7c6a5202eb558382f616e6072a0c4e9100a1a7e65d2629da7a6f86`. The image remains temporary and untracked, as required by the zero-binary fixture policy.
- Before and after a cache-bypassing reload, the renderer exposed exactly the frozen `aiVideoAssembly` bridge with one key: `getFoundationStatus`.
- Real IPC returned `{ ok: true, value: { repositoryFoundation: "ready" } }` before and after reload.
- `typeof process`, `typeof require`, and `typeof Buffer` were all `undefined` before and after reload.
- The document contained zero buttons, inputs, selects, textareas, or links. Visual inspection found no media, transcript, timeline, AI, persistence, render, or export interface.
- Closing the window removed the only DevTools page target and renderer. M0.1 defines no background product worker. On macOS, the idle app/GPU/network processes remained until explicit app termination; after SIGINT, no matching process or listener on port `9224` remained.

## Applicability note

M0.1 explicitly excludes packaging, installers, signing, and distribution. Therefore no packaged application artifact exists to test. The compiled development build is the applicable artifact for this milestone, and it launched without a blank screen.

## Environmental occurrences

- An initial archive command was invoked from the empty temporary destination and failed with `not a git repository` before copying or executing project code. The command was rerun from the repository into the same still-empty destination.
- The first sandboxed install encountered registry `ENOTFOUND` and was interrupted before completion. That partial directory was abandoned; the authoritative clean-room above was newly created and passed with approved network access.
- macOS display capture was unavailable to `screencapture` in this environment. `Page.captureScreenshot` through the Electron DevTools protocol succeeded and produced the inspected renderer image.

No occurrence represents a product failure or changed tracked repository state.

`M0.1 MANUAL SMOKE: PASS — M0.1 FROZEN 2026-07-22`
