# M0.1 — Manual Smoke Checklist

Status: PENDING

M0.1 has no editing workflow. The smoke validates the desktop shell and security-visible behavior only.

- [ ] App launches from a clean install.
- [ ] Main window renders the neutral repository-foundation screen.
- [ ] Reload does not expose Node globals to renderer code.
- [ ] DevTools inspection confirms no unrestricted privileged API on `window`.
- [ ] Closing the window terminates background workers cleanly.
- [ ] No media, transcript, AI, persistence, or export UI is present.
- [ ] Packaged development build, when applicable, starts without a blank screen.

Result: PENDING
