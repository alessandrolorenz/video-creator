# M1.0 — Implementation-Judge Correction

Status: IMPLEMENTED AND VERIFIED LOCALLY — publication and re-judge not authorized

- Correction date: 2026-07-22 (America/Sao_Paulo)
- Failed candidate: `a67538edd9f1df91e4790e6795c1b16ca6e3ce2f`
- Judge evidence: `docs/specs/M1.0-independent-judge-report.md`
- Scope: the three blocking findings and two narrow nonblocking contract defects reported by the independent judge
- Excluded: publication, hosted CI, re-judge, real `ffprobe`, private-input inspection, manual smoke, freeze, and M1.1

## Authorization and gate

The product owner authorized the narrow correction after the independent implementation-judge `FAIL`. This artifact records only local implementation and evidence. The judge verdict remains `FAIL` until a new independent judge evaluates an exact published correction SHA after hosted CI passes.

## Corrective mapping

| Judge finding | Test-first reproduction | Minimal correction | Acceptance evidence |
|---|---|---|---|
| Subprocess cancellation, timeout, and output-limit completion resolved before `close`. | Existing bounded-process terminal tests were changed to require the returned promise to remain pending until the fake child emits `close`. | Terminal outcomes are latched before kill; data, abort, and timer sources are detached; completion occurs only from `close`. A kill failure cannot replace the already-latched result. | Focused bounded-process tests pass for cancellation, timeout, output cap, and kill failure with explicit close-wait assertions. |
| Cancellation was not latched across utility-client configuration and immediate replacement. | Added cancel-before-configuration and cancel-after-send replacement races. The integration harness now cancels a slow probe and requests its replacement immediately. | The client settles cancellation locally, does not send an unsent cancelled probe, and queues a replacement until an already-sent cancelled job reaches a terminal worker result. The worker treats a well-formed late cancel with no active job as an idempotent no-op while retaining wrong-job rejection during active work. | Focused client/worker tests and the compiled real Electron `utilityProcess` integration pass using text-only fake executables and files. |
| Renderer omitted required metadata and warnings. | Added ready-state assertions for real/base frame rate, time base, rotation, audio details, and every closed warning code. | The ready summary now displays all frozen rational/rotation/audio fields and exact warning codes, including an explicit empty-warning state. | Renderer unit tests pass without paths, opaque IDs, or raw probe output entering the view. |
| Cancellation-only codes were admitted as renderer errors. | Added boundary cases that reject the three cancellation-only codes as error payloads. | Removed `DIALOG_CANCELLED`, `PROBE_CANCELLED`, and `TRANSCRIPT_CANCELLED` from the closed renderer failure-code set; typed cancellation outcomes remain unchanged. | Shared IPC schema tests pass. |
| Transcript file-selection errors offered the video retry route. | Added session and rendered-label tests for transcript-origin `FILE_UNAVAILABLE`. | Renderer session state records the operation-specific retry target instead of inferring ownership only from the error-code prefix. | Renderer session and component tests pass for preserved-media transcript retry. |

## Test-first evidence

1. The first focused red gate ran the five changed bounded-process, client, renderer, renderer-session, and IPC suites: 57 tests ran and 11 failed for the expected missing correction behavior.
2. After the minimal fixes, those same five suites passed all 57 tests.
3. A separate worker regression first proved that a late terminal-race cancel emitted an unwanted protocol error; the worker no-op correction then made the expanded focused set pass 63 tests across six files.
4. The real compiled Electron regression passed with immediate replacement after cancellation. It used temporary text-only fakes, no real `ffprobe`, no media fixture, no network, and no private input.

## Private manual-smoke inputs and fixture guard

The product owner identified `videos-teste/` as local video, transcript, and reference-cut material reserved for a future authorized manual smoke. The directory is now explicitly ignored and remains uninspected and untracked.

The architecture guard now obtains its fixture-audit inventory from Git: tracked files plus nonignored untracked files. This prevents the guard from traversing privacy-approved ignored local inputs while preserving enforcement for every tracked file, including a force-added file that matches an ignore rule. A temporary-repository regression proves both sides of that boundary.

## Authoritative local verification

The following sequence passed after all executable correction edits and before the local correction commit. Recording these results changed documentary evidence only.

| Command | Result |
|---|---|
| `env CI=true pnpm install --frozen-lockfile` | PASS — all 8 workspaces; lockfile already up to date |
| `env CI=true pnpm format:check` | PASS |
| `env CI=true pnpm lint` | PASS — zero warnings |
| `env CI=true pnpm check:boundaries` | PASS |
| `env CI=true pnpm typecheck` | PASS — root, seven participating workspaces, and dedicated worker target |
| `env CI=true pnpm test` | PASS — complete Vitest suite, including real Electron `utilityProcess` integration with text-only fakes |
| `env CI=true pnpm build` | PASS — TypeScript `6.0.3`; main, worker, preload, renderer, and all pure packages |

The first auxiliary `pnpm exec prettier --write ...` call was issued without `CI=true`; pnpm's dependency-status guard attempted a noninteractive relink and aborted before formatting. The authoritative frozen install restored the already-locked workspace links with no dependency or lockfile change, after which formatting and all required checks passed. This was an environment invocation error, not a product failure.

## Rollback and next gate

The correction is confined to process termination, probe-client/worker cancellation sequencing, renderer presentation/session routing, the closed IPC error set, repository inventory policy, tests, and evidence. It adds no dependency, lockfile change, AI/cloud behavior, persistence, editing, rendering, export, packaging, or real-tool invocation.

After local verification and commit, the next required authorization is publication of the documentary judge report and correction commits. A green hosted CI run on the exact correction SHA is required before a new independent implementation judge. Real-input smoke remains gated until that judge passes and the product owner separately authorizes external `ffprobe` use and private inputs.
