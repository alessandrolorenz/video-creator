# M1.0 — Manual Smoke Checklist

Status: IN PROGRESS (owner-run) — independent re-judge `PASS WITH NOTES`; real-input smoke has begun and is incomplete. The agent still must not install, locate, or execute `ffprobe`, nor inspect/enumerate/hash/process the ignored private inputs.

- [x] Privacy-approved ignored local media/transcript/reference inputs are identified without inspecting or committing their contents.
- [ ] External local `ffprobe` prerequisite and sanitized version are recorded.
- [ ] Fresh install/build launches a nonblank ingest screen.
- [ ] Cancelling media and transcript dialogs preserves stable state.
- [ ] Valid MP4/MOV-family media imports with sanitized metadata matching direct `ffprobe` output.
  - Partial (2026-07-23, owner-run): one real MP4 imported and the renderer displayed sanitized metadata — duration, size, video/audio codecs, dimensions, frame-rate rationals, time base, container, `Warnings: None`. This supports the media selection + probe path only. The **direct command-line `ffprobe` comparison has not been performed**, so this item stays unchecked.
- [ ] An active probe can be cancelled and leaves no worker/`ffprobe` process.
- [ ] Valid word- or segment-timed JSON imports and reaches `ready`.
- [ ] Malformed, overlapping, and out-of-bounds transcripts fail safely without losing valid media state.
- [ ] Replacing media invalidates the prior transcript.
- [ ] DevTools exposes no path, Node global, raw IPC, filesystem/process/environment/command API, or unrestricted bridge.
- [ ] Window/app close leaves no worker or `ffprobe` process.
- [ ] No tracked file or source input is modified.
- [ ] No M1.1–M1.3 UI or behavior is present.

Result: PENDING (in progress; every unchecked item above is `PENDING MANUAL OBSERVATION`)

Current status: the product owner has begun owner-run manual smoke on their own machine — external `ffprobe` is resolved and one real MP4's sanitized metadata reached the renderer with `Warnings: None`. That single observation supports only the media selection + probe path. All other items remain `PENDING MANUAL OBSERVATION`, and even the media item stays unchecked until its direct `ffprobe` comparison is recorded. The agent still must not install, locate, or execute `ffprobe`, nor inspect/enumerate/hash/process the ignored private inputs; owner-run smoke does not grant the agent those actions.
