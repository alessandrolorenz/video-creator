# M1.0 — Independent Corrected-Plan Re-Judge Report

Status: FINAL — FAIL; two narrow normative/test-first corrections required

- Judge date: 2026-07-22 (America/Sao_Paulo)
- Reviewed commit: `635af9be792c9f3231d56ee1447c8dc37b1106d0`
- Reviewed state: `main`, identical to `origin/main`, clean index/worktree
- Review mode: independent and read-only; no repository or Git state modified
- Exact CI: GitHub Actions run `29929747625` — PASS
- Verdict: **FAIL**

## Verdict

The correction fully resolved the first judge's five blocking findings. All 30 result/outcome rows, all four warning rows, ownership boundaries, and the seven-checkpoint separation are present. Two smaller decisions still remain for implementation-time invention: the exact duration-string grammar and explicit capability-cache/retry tests. Checkpoint 1 is therefore not ready.

## Evidence table

| # | Result | Independent evidence |
|---:|---|---|
| 1 | PASS | M1.0 ends at ingest/validation; matching, timeline, preview, render, and export remain M1.1–M1.3. |
| 2 | **FAIL** | Traceability for 30 results and four warnings is complete, but exact duration grammar and explicit capability cache/retry test rows are missing. |
| 3 | PASS | Transcript units, rounding, limits, shape, IDs, order, overlap, and bounds are assigned to CP1/CP4. |
| 4 | **FAIL** | Rational, duration selection, VFR, rotation, and warnings are otherwise deterministic, but “strict finite positive base-10 decimal syntax” has no exact grammar. |
| 5 | PASS | No dependency, installation, download, or bundling of `ffprobe`. |
| 6 | PASS | Main → utility process → subprocess is correctly separated across CP3–CP5. |
| 7 | PASS | Executable, environment, paths, arguments, stderr, diagnostics, and handles remain privileged. |
| 8 | PASS | No shell, generic command runner, filesystem bridge, raw IPC, or renderer-controlled arguments. |
| 9 | PASS | Timeout, caps, cancellation, error/close races, crashes, disconnect, and shutdown have owners and tests. |
| 10 | PASS | Dialogs, canonicalization, validation, and bounded reads are main-owned in CP4. |
| 11 | PASS | States, polling ≥250 ms, stable-state stop, accessibility, and cleanup are in CP6. |
| 12 | PASS | Guard evolution is test-first and preserves pure packages, empty export, and import/dependency restrictions. |
| 13 | PASS | No registry dependency, binary, LFS, download, AI, database, packaging, or future feature. |
| 14 | **FAIL** | Checkpoints are test-first, but cache hit/keying/failure-not-cached/retry and duration grammar boundary cases are not explicit. |
| 15 | PASS | Seven checkpoints are separately authorized, committable, verifiable, and reversible. |
| 16 | PASS | CP7 defines clean archive, hashes, frozen install, full sequence, and output inventory; CI is a separate gate. |
| 17 | PASS | Local `ffprobe` installation, privacy, and real-input smoke remain separately authorized. |
| 18 | PASS | The plan stops before implementation and does not infer M1.1 authorization. |

## Result and warning matrix review

All 30 results/outcomes have an owner, checkpoint, positive test, negative test, and precedence/race rule: `INVALID_REQUEST`, `DIALOG_CANCELLED`, `FILE_UNAVAILABLE`, `FILE_NOT_REGULAR`, `MEDIA_EMPTY`, `MEDIA_UNSUPPORTED`, `FFPROBE_CONFIGURATION_INVALID`, `FFPROBE_NOT_FOUND`, `FFPROBE_INCOMPATIBLE`, `PROBE_TIMEOUT`, `PROBE_CANCELLED`, `PROBE_OUTPUT_LIMIT`, `PROBE_FAILED`, `PROBE_OUTPUT_INVALID`, `VIDEO_STREAM_MISSING`, `AUDIO_STREAM_MISSING`, `MEDIA_DURATION_INVALID`, `TRANSCRIPT_PREREQUISITE_MISSING`, `TRANSCRIPT_TOO_LARGE`, `TRANSCRIPT_ENCODING_INVALID`, `TRANSCRIPT_JSON_INVALID`, `TRANSCRIPT_SCHEMA_UNSUPPORTED`, `TRANSCRIPT_LIMIT_EXCEEDED`, `TRANSCRIPT_ENTRY_INVALID`, `TRANSCRIPT_ORDER_INVALID`, `TRANSCRIPT_OVERLAP_UNSUPPORTED`, `TRANSCRIPT_OUT_OF_BOUNDS`, `TRANSCRIPT_CANCELLED`, `WORKER_UNAVAILABLE`, and `INTERNAL_ERROR`.

All four warnings have an owner, CP2 positive/suppression tests, fixed order, and deduplication: `DURATION_FALLBACK_USED`, `VARIABLE_FRAME_RATE_SUSPECTED`, `RATE_METADATA_MISSING`, and `ROTATION_METADATA_PRESENT`.

`MEDIA_DURATION_INVALID` and `DURATION_FALLBACK_USED` remain subject to the missing exact duration grammar.

## Blocking findings

1. **Duration grammar remains ambiguous.** The normative package does not decide `+1`, `01`, `.5`, `1.`, `1e3`, or surrounding whitespace, which can change fallback, warnings, and terminal classification.
2. **Capability cache/retry is not fully test-first.** CP3 does not explicitly require same-key cache-hit/no-spawn, exact keying, failure-not-cached/retry, or successful-retry-then-hit tests.

## Non-blocking notes

- CP5 should state that its temporary fake executable supports the current POSIX macOS/Ubuntu targets or define a future cross-platform strategy.
- Real polling behavior belongs to CP6; CP5's polling reference is only the bridge/snapshot contract.
- The current guard still has M0.1-specific messages, already assigned for test-first evolution in CP1.

## Scope creep

None.

## Required corrections

1. Freeze an exact duration-string grammar and classification for every rejected form.
2. Add duration lexical/boundary/classification cases to CP2.
3. Add explicit capability cache key/hit/failure/retry/replacement cases to CP3.

## Readiness

**DO NOT AUTHORIZE CHECKPOINT 1.** The remaining corrections are small but must become normative and pass another independent plan judgment before implementation.

`M1.0 CORRECTED-PLAN RE-JUDGE: FAIL — CHECKPOINT 1 NOT AUTHORIZED`
