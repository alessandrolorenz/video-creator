# M1.0 — Freeze Report

Status: BLOCKED — implementation and re-judge passed; real-input manual smoke has begun (owner-run) but is incomplete; explicit freeze approval pending

## Final verdict

PENDING — independent re-judge is `PASS WITH NOTES`, but manual smoke and freeze approval remain pending.

## Frozen baseline

Implementation checkpoints 1–7 are published. CP7 baseline `a67538edd9f1df91e4790e6795c1b16ca6e3ce2f` had green hosted CI before the failed judge. Correction SHA `d76155aa63427b8b55d2d7c769c28a5982bd49aa` is published but not a freeze candidate because its hosted CI failed.

## Automated evidence

Correction run `29960043463` passed install, format, lint, boundaries, and typecheck, then failed seven stale higher-level fake-process lifecycle tests; 391/398 passed and build was skipped. Follow-up `8c8b85b7373a370049112387e2fcdbd5b7a722a6` passes the complete local sequence and hosted run `29961423406` with 398/398 tests and build. Final judged candidate `f044b8ddd7b52768086935601c3c40517b906d1a` passed hosted run `29961604142`.

## Independent judge evidence

The first independent judge returned `FAIL` on the CP7 candidate. Re-judge exact candidate `f044b8ddd7b52768086935601c3c40517b906d1a` passed hosted run `29961604142` and received `PASS WITH NOTES`; see `docs/specs/M1.0-independent-rejudge-report.md`.

## Manual smoke evidence

Begun (owner-run), incomplete. On 2026-07-23 the product owner resolved external `ffprobe` and selected one real MP4; the renderer displayed sanitized metadata (duration, size, video/audio codecs, dimensions, frame-rate rationals, time base, container, `Warnings: None`), proving the real video-selection + probe path for that single input. Not yet recorded: direct command-line `ffprobe` comparison, transcript import, malformed/overlapping/out-of-bounds handling, cancellation, replacement invalidation, DevTools privacy, process cleanup, source immutability, and regression absence — all `PENDING MANUAL OBSERVATION`. The agent still may not install/execute `ffprobe` or inspect the ignored private inputs, and no private filenames, transcript text, absolute paths, environment values, or raw `ffprobe` output are recorded.

## Known limitations

M1.0 currently provides ingest/status only. No passage matching, timeline editing, preview, render, export, persistence, AI, or multi-asset behavior is present.

## Next authorization

Obtain explicit authorization for the external `ffprobe` prerequisite and privacy-approved real-input manual smoke. If smoke passes, record evidence and request explicit M1.0 freeze approval.
