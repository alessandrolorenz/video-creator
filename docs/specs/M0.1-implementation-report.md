# M0.1 — Implementation Report

Status: M0.1 FROZEN — PASS

## Authorization

- M0.0 approval / freeze: APPROVED / FROZEN — 2026-07-21
- M0.1 spec freeze: APPROVED / FROZEN — 2026-07-21
- Amendment 001: APPROVED / APPLIED — 2026-07-21
- Final plan judge: PASS WITH NOTES — no further plan judge required
- Checkpoint 0: EXPLICITLY AUTHORIZED / COMPLETE
- Checkpoint 1: EXPLICITLY AUTHORIZED / COMPLETE
- Checkpoint 2: EXPLICITLY AUTHORIZED / COMPLETE
- Checkpoint 3: EXPLICITLY AUTHORIZED / COMPLETE
- Checkpoint 4: EXPLICITLY AUTHORIZED / COMPLETE
- Checkpoint 5: EXPLICITLY AUTHORIZED / COMPLETE

## Checkpoint authorizations

The product owner explicitly authorized Checkpoint 0, including the external manifest, Git initialization, baseline commit, and evidence commit.

The product owner explicitly authorized Checkpoint 1:

> Autorizo o Checkpoint 1 do M0.1 — toolchain, monorepo, workspace skeleton, fixture policy, instalação de dependências, lockfile e commit local.

The product owner explicitly authorized Checkpoint 2:

> Autorizo o Checkpoint 2 do M0.1 — domínio, contratos provider-neutral, testes e architecture/fixture guard, com commit local.

The product owner explicitly authorized Checkpoint 3:

> Autorizado! Prossiga.

The product owner explicitly authorized Checkpoint 4 by confirming the Checkpoint 3 tests and asking to continue:

> tudo certo vamos prosseguir

The product owner explicitly authorized Checkpoint 5:

> Aprovado

## Immutable baseline

| Item | Evidence |
|---|---|
| Repository | `/Users/alessandrolorenz/Documents/github/ai-video-editor` |
| Branch | `main` |
| Baseline commit | `b94ac10e04632b0e64d36d1b0655d56494c7a715` |
| Checkpoint 0 evidence commit | `eb9c602` |
| Baseline tree | Exactly 32 Markdown files |
| External manifest | `/private/tmp/ai-video-editor-M0.1-pre-git.sha256` |
| Manifest entries / digest | 32 / `cf370451ee052b987d6344020fac69f59eb2e8b5f1997d9e75f4db8b2cf5962d` |
| Commit comparison | PASS — no differences |

## Checkpoint 1 implementation summary

- Created the pnpm workspace with one root, one desktop app, and five framework-independent packages.
- Added strict shared TypeScript configuration.
- Added ESLint flat configuration, Prettier, Vitest baseline, EditorConfig, and repository ignore policy.
- Pinned Node `24.18.0` and pnpm `11.9.0` policy.
- Added the approved repository fixture policy with a zero-byte M0.1 binary-fixture budget.
- Reserved `packages/export` as metadata plus an empty barrel with no public symbols.
- Installed only the approved React/Electron/tooling/runtime-schema dependencies.
- Generated and verified `pnpm-lock.yaml` with exact direct dependency versions.
- Added no domain contracts, worker contracts, AI implementation, media execution, Electron shell, renderer, persistence, export implementation, CI, or binary fixture.

## Checkpoint 1 files

New repository files:

- `.editorconfig`
- `.gitignore`
- `.nvmrc`
- `.prettierignore`
- `package.json`
- `pnpm-workspace.yaml`
- `pnpm-lock.yaml`
- `tsconfig.base.json`
- `eslint.config.js`
- `prettier.config.js`
- `vitest.config.ts`
- `apps/desktop/package.json`
- `packages/domain/package.json`
- `packages/media/package.json`
- `packages/ai-contracts/package.json`
- `packages/timeline/package.json`
- `packages/export/package.json`
- `packages/export/src/index.ts`
- `docs/fixture-policy.md`

Evidence/status updates:

- `README.md`
- `docs/specs/M0.1-repository-audit.md`
- `docs/specs/M0.1-implementation-plan.md`
- `docs/specs/M0.1-implementation-report.md`

## Exact dependency decisions

| Package | Exact version | Purpose |
|---|---:|---|
| Electron | `43.2.0` | Future desktop runtime; no shell created in CP1 |
| React / React DOM | `19.2.8` | Future renderer |
| Zod | `4.4.3` | Future runtime IPC validation |
| Vite | `8.1.5` | Future renderer build |
| `@vitejs/plugin-react` | `6.0.3` | Future React/Vite integration |
| Vitest | `4.1.10` | Test harness baseline |
| TypeScript alias package | `@typescript/typescript6@6.0.2` | Supported compiler API and `tsc6` binary |
| ESLint / `@eslint/js` | `10.7.0` / `10.0.1` | Static analysis |
| `typescript-eslint` | `8.65.0` | TypeScript lint integration |
| Prettier | `3.9.6` | Formatting |
| React Hooks / Refresh lint plugins | `7.1.1` / `0.5.3` | Future renderer rules |
| Node types | `24.13.3` | Node 24 type surface |
| React / React DOM types | `19.2.17` / `19.2.3` | Future renderer types |

All direct dependency specifications are exact; no `latest`, caret, tilde, wildcard, AI-provider, FFmpeg, database, packaging, telemetry, or filesystem-abstraction dependency exists.

## Checkpoint 2 implementation summary

- Added branded non-negative safe-integer `TimeUs` construction with typed validation errors and an explicit throwing helper for trusted call sites.
- Added validated half-open `SourceRange` values with positive duration, containment, and source-duration-bound checks, all in integer microseconds.
- Added the versioned timeline v1 foundation contract, restricted to an empty track list and zero duration, with runtime structural validation and JSON round trips.
- Added media worker-job interfaces for progress, cancellation, success, cancellation result, and typed failure; no worker implementation or process integration was added.
- Added provider-neutral AI request/result/provider placeholders; no vendor SDK, model, transport, secret, or execution implementation was added.
- Added package typecheck/build configuration and a single executable repository guard covering manifests, workspace edges, re-exports, static imports, dynamic `import()`, CommonJS `require()`, relative cross-package imports, framework/provider imports, the reserved export package, binary content, prohibited fixture paths, and Git LFS.
- Added negative architecture fixtures created only in temporary directories during tests; no binary fixture was committed.
- Added the root `@types/node` declaration at the already pinned `24.13.3` version so architecture tests and the Node-based guard are typechecked directly.

## Checkpoint 2 test-first evidence

1. Tests and expected contracts were created before their implementations.
2. The first attempted `pnpm test` was stopped by pnpm because changed manifests required a non-interactive install reconciliation; this was an environment precondition rather than a test result.
3. After lockfile/install reconciliation under Node `24.18.0`, direct Vitest execution produced the intended red state: four suites failed because `time.ts`, `source-range.ts`, timeline implementation, and `check-boundaries.mjs` did not yet exist; two type-contract suites passed.
4. The minimal implementations were added and the suite became green at 37 tests.
5. Boundary review added explicit coverage for bare Node built-ins, type-only domain edges, root and package manifest-only violations, an import-free export package, and disguised binary content; final evidence is 48 passing tests across 6 files.

## Checkpoint 2 files and boundaries

- `packages/domain`: branded time, source ranges, barrel, tests, and TypeScript configs.
- `packages/timeline`: empty v1 contract/validator, tests, domain workspace dependency, and TypeScript configs.
- `packages/media`: worker-job type contracts, barrel, tests, and TypeScript configs.
- `packages/ai-contracts`: provider-neutral interfaces, tests, and TypeScript configs.
- `packages/export`: build/typecheck configs only; its original barrel remains symbol-free and import-free.
- `scripts/check-boundaries.mjs` plus its declaration.
- `tests/architecture/check-boundaries.test.ts` with temporary negative repositories.
- Root manifest/lockfile changes limited to the boundary command, root Node types, and the timeline-to-domain workspace edge.

No Electron source, renderer, IPC, persistence, media execution, AI execution, export implementation, CI workflow, or binary fixture was added.

## Checkpoint 3 implementation summary

- Added the Electron main process with local-file loading only, denied popup creation, denied unexpected navigation, and explicit `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, and `webSecurity: true` window settings.
- Added a bundled CommonJS sandbox-compatible preload artifact at `dist/preload/index.cjs`.
- Exposed exactly one frozen bridge method: `window.aiVideoAssembly.getFoundationStatus()`.
- Added a shared versioned request and typed success/error response contract.
- Added strict Zod runtime validation of `unknown` IPC payloads before the status reader is invoked.
- Added stable invalid-request and internal-error envelopes without leaking caught error details.
- Added a neutral React repository-foundation screen with no file picker, media, transcript, timeline, AI, persistence, render, or export controls.
- Added a restrictive renderer Content Security Policy and relative production asset URLs compatible with `BrowserWindow.loadFile`.
- Added separate main, preload, and renderer typecheck/build targets without adding dependencies or changing the frozen lockfile.

## Checkpoint 3 test-first and build evidence

1. Five desktop test files were created before production sources; the intended red run failed because all policy, handler, bridge, renderer, preload, main, declaration, and HTML files were absent.
2. The first CommonJS-source approach used `.cts`; Vitest/Rolldown did not transpile that extension and exposed the incompatibility before acceptance.
3. The preload was changed to a dedicated Vite CommonJS bundle while retaining ordinary TypeScript source and the shared typed contract.
4. Initial artifact inspection found `index.cjs.cjs` and absolute renderer `/assets` URLs; the bundle file-name function and Vite `base: './'` corrected both before smoke.
5. The first root typecheck rejected renderer JSX because the frozen root config does not set `jsx`; the desktop retained React while using `createElement`, allowing both root and renderer-specific strict typechecks to pass without changing Checkpoint 0–2 configuration.
6. Final evidence is 16 passing desktop tests and 64 passing tests across 11 repository test files.

## Checkpoint 3 files and boundaries

- `apps/desktop/src/main`: Electron entry, security/window policy, validated foundation-status handler, and tests.
- `apps/desktop/src/preload`: one-method bridge, sandbox preload entry, and bridge test.
- `apps/desktop/src/shared/ipc.ts`: channel, Zod request schema, request/result/error types, and renderer bridge type.
- `apps/desktop/src/renderer`: neutral React shell, browser-only declaration, CSS, entry, and render test.
- `apps/desktop/index.html`: restrictive CSP and local renderer entry.
- Desktop TypeScript and Vite configs plus package build/typecheck/start scripts.

No root/package implementation, provider SDK, media execution, persistence, export implementation, installer, CI workflow, binary fixture, secret, environment bridge, generic execution bridge, shell bridge, or filesystem bridge was added.

## Checkpoint 4 implementation summary

- Added `.github/workflows/ci.yml` on Ubuntu 24.04 with a 15-minute timeout and repository contents read-only permission.
- Added push-to-main, pull-request, and manual-dispatch triggers with duplicate-run cancellation.
- Pinned every third-party action to a full immutable commit SHA and disabled persisted checkout credentials.
- Configured pnpm from the root `packageManager` field, Node from `.nvmrc`, and pnpm caching from the committed lockfile.
- Reproduced the required sequence exactly: frozen install, formatting, lint, boundaries, typecheck, tests, and build.
- Added operational README prerequisites, install commands, complete verification commands, desktop launch command, current-scope warning, workspace links, and CI link.
- Added no dependency, lockfile change, application behavior, packaging, deployment, publication, or remote push.

## Checkpoint 4 action pins

| Action | Release | Immutable commit |
|---|---:|---|
| `actions/checkout` | `v7.0.1` | `3d3c42e5aac5ba805825da76410c181273ba90b1` |
| `pnpm/action-setup` | `v6.0.8` | `0e279bb959325dab635dd2c09392533439d90093` |
| `actions/setup-node` | `v7.0.0` | `820762786026740c76f36085b0efc47a31fe5020` |

The release tags and dereferenced commit for the annotated pnpm tag were verified directly against the official GitHub repositories on 2026-07-21.

## Checkpoint 5 clean-room source

| Item | Evidence |
|---|---|
| Source commit | `1227576e8468d8a076f313a89ed2a4d3421296bf` — completed Checkpoint 4 |
| Source branch | `main` |
| Original worktree | Clean before archive creation |
| Population method | `git archive --format=tar HEAD` extracted into a new temporary directory |
| Clean-room directory | `/private/tmp/ai-video-editor-M0.1-cp5.o6GlLB` |
| Git metadata | Absent |
| Initial dependencies / local store | No `node_modules`; no `.pnpm-store` |
| Tracked-file comparison | Exact match: 100 archive files / 100 Git tree files |
| Pre-install SHA-256 manifest | `/private/tmp/ai-video-editor-M0.1-cp5-source.sha256` |
| Manifest entries / digest | 100 / `fe5ccc8445a18e8a52ad5fdb94e05d01f5f14c9d847f4e58761bfee491008396` |

## Checkpoint 5 environment and exact versions

| Component | Version |
|---|---:|
| macOS / architecture | `26.5.2` build `25F84` / `arm64` |
| Node.js | `24.18.0` |
| pnpm | `11.9.0` |
| TypeScript alias package / compiler | `6.0.2` / `6.0.3` |
| Vitest | `4.1.10` |
| Vite | `8.1.5` |
| Electron | `43.2.0` |
| React | `19.2.8` |
| Zod | `4.4.3` |

## Checkpoint 5 timed clean-room reproduction

Every required command passed on its first attempt. Times below are `/usr/bin/time -p` real elapsed seconds.

| Command | Result | Real time |
|---|---|---:|
| `pnpm install --frozen-lockfile` | PASS — 189 packages linked; lockfile supply-chain policy passed; no resolution change | `0.73s` |
| `pnpm format:check` | PASS | `0.86s` |
| `pnpm lint` | PASS — zero warnings | `1.33s` |
| `pnpm check:boundaries` | PASS | `0.30s` |
| `pnpm typecheck` | PASS — root and all participating workspaces | `2.56s` |
| `pnpm test` | PASS — 11 files, 64 tests | `1.48s` |
| `pnpm build` | PASS — desktop main/preload/renderer and all framework-independent packages | `2.46s` |
| **Total measured real time** | **PASS** | **`9.72s`** |

Failures: none. Retries: none.

## Checkpoint 5 mutation and artifact audit

- All 100 pre-install source hashes verified `OK` after install, checks, tests, and build.
- The non-generated file inventory remained an exact 100-file match with the Checkpoint 4 Git tree.
- Installation created root `node_modules` (`137M`) plus workspace dependency links; no workspace `.pnpm-store` was created.
- Build created only ignored `dist` outputs: desktop `660K` / 17 files; AI contracts `12K` / 3; domain `36K` / 9; export `12K` / 3; media `24K` / 6; timeline `12K` / 3.
- No coverage directory, log file outside dependencies, fixture, `packages/fixtures`, `.gitattributes`, Git LFS pointer/configuration, dependency change, lockfile change, or tracked-source mutation was produced.
- Built security artifacts were present and inspected: CommonJS preload `dist/preload/index.cjs`; renderer HTML retained the restrictive CSP and relative asset URLs.

## Verification evidence

| Command / check | Result |
|---|---|
| Frozen install under Node `24.18.0` | PASS — all 7 workspace projects, lockfile already up to date |
| `pnpm list --recursive --depth -1` | PASS — root + desktop + 5 packages discovered |
| `pnpm run format:check` | PASS |
| `pnpm run lint` | PASS |
| `pnpm run typecheck` | PASS — strict config compiled with `tsc6` |
| `pnpm run test` | PASS — no test files expected in CP1 |
| `pnpm run build` | PASS — toolchain/workspace build smoke only |
| `tsc6 --showConfig -p tsconfig.base.json` | PASS — strict options resolve |
| Exact direct-version validation | PASS — all 7 manifests use exact specs |
| Fixture-policy heading inspection | PASS — all Amendment 001 sections present |
| `packages/fixtures` inspection | PASS — absent |
| Binary-fixture extension scan | PASS — no matches outside ignored dependencies/Git |
| Git LFS configuration scan | PASS — no `.gitattributes` outside Git metadata |
| Forbidden direct-dependency scan | PASS — no matches |

## Checkpoint 2 verification evidence

| Command / check | Result |
|---|---|
| Frozen install under Node `24.18.0` | PASS — all 7 workspace projects; lockfile already up to date; pnpm `11.9.0` |
| Prettier check | PASS — all matched files formatted |
| ESLint with zero warnings | PASS |
| `node scripts/check-boundaries.mjs` | PASS — repository boundaries accepted |
| Strict root and package typecheck | PASS — domain, timeline, media, AI contracts, and export |
| Vitest | PASS — 6 test files, 48 tests |
| Package build | PASS — all 5 framework-independent packages |
| `git diff --check` | PASS |
| `packages/fixtures` / `.gitattributes` inspection | PASS — both absent |
| Negative import/manifest fixtures | PASS — static, re-export, dynamic, CommonJS, relative, undeclared, manifest-only edge, root forbidden dependency, runtime type-boundary, Node/framework/provider, and export-package cases rejected |
| Negative fixture-policy cases | PASS — extension, disguised NUL content, LFS pointer, and LFS configuration rejected from temporary repositories |

## Checkpoint 3 verification evidence

| Command / check | Result |
|---|---|
| Frozen install under Node `24.18.0` | PASS — all 7 workspace projects; lockfile already up to date; pnpm `11.9.0` |
| Prettier check | PASS — all matched files formatted |
| ESLint with zero warnings | PASS |
| `node scripts/check-boundaries.mjs` | PASS |
| Strict root and recursive workspace typecheck | PASS — desktop main, preload, renderer, and all prior packages |
| Vitest | PASS — 11 test files, 64 tests; 5 files and 16 tests belong to desktop |
| Root recursive build | PASS — Electron main, CommonJS preload, renderer, and all prior packages |
| Built artifact inspection | PASS — `dist/preload/index.cjs`; renderer HTML uses relative JS/CSS assets and retains restrictive CSP |
| Automated launch smoke | PASS — built Electron renderer reported `Secure desktop foundation ready.` through real IPC |
| Runtime bridge inspection | PASS — only `getFoundationStatus` exposed |
| Runtime renderer isolation | PASS — `typeof process`, `typeof require`, and `typeof Buffer` all returned `undefined` |
| Process termination | PASS — Electron was intentionally stopped with SIGINT after smoke inspection; no worker exists in M0.1 |
| `git diff --check` | PASS |

## Checkpoint 4 verification evidence

| Command / check | Result |
|---|---|
| Workflow YAML parse | PASS — Ruby/Psych loaded the workflow as a mapping |
| Immutable-action pin scan | PASS — all 3 `uses` entries contain 40-character commit SHAs |
| CI command-order inspection | PASS — the 7 required pnpm commands occur in the planned order |
| Frozen install under Node `24.18.0` | PASS — all 7 workspaces; pnpm `11.9.0`; lockfile already up to date |
| `pnpm format:check` | PASS |
| `pnpm lint` | PASS — zero warnings |
| `pnpm check:boundaries` | PASS |
| `pnpm typecheck` | PASS — root plus all participating workspaces |
| `pnpm test` | PASS — 11 files, 64 tests |
| `pnpm build` | PASS — desktop main/preload/renderer plus framework-independent packages |
| README command/link inspection | PASS — every documented command exists and every workspace/CI target exists |
| `git diff --check` | PASS |

## Checkpoint 5 consolidated security assertions

| Assertion | Evidence |
|---|---|
| Renderer isolation | Desktop security/window/preload tests pass; prior real launch smoke returned `undefined` for `process`, `require`, and `Buffer` |
| Narrow bridge | Exactly `getFoundationStatus`; frozen bridge test and prior runtime inspection pass |
| Runtime validation before work | Invalid/extra/missing request tests pass and prove the status reader is not called |
| Local content and navigation | `loadFile`, same-file reload only, denied popup and unexpected navigation tests pass |
| Restrictive CSP | Source/static test and clean-room built HTML inspection pass |
| Framework-independent boundaries | Repository guard and negative architecture fixtures pass |
| Provider/media exclusions | No provider SDK, FFmpeg dependency/execution, binary fixture, or export implementation exists |
| CI hardening | Read-only token permission, non-persisted credentials, full action SHAs, timeout, and concurrency cancellation present |

## TypeScript package evidence

- Manifest/lockfile alias: `typescript: npm:@typescript/typescript6@6.0.2`.
- Installed package metadata: `6.0.2`.
- Installed binary: `tsc6`.
- `tsc6 --version`: `6.0.3`.
- Programmatic `typescript.version`: `6.0.3`.
- Programmatic `createProgram`: available as a function.

The package version and bundled compiler version differ by one patch, but the CLI/API agree with each other and remain inside the supported TypeScript `<6.1.0` range. The exact approved alias package remains locked at `6.0.2`.

## Environment notes

- The interactive shell reports Node `v24.18.0`, matching `.nvmrc` and the engine policy.
- Codex's fallback `pnpm` wrapper internally uses a bundled Node `v24.14.0`, which emits an engine warning for normal script runs.
- The required frozen-install verification was therefore also executed by invoking pnpm `11.9.0` explicitly under Node `v24.18.0`; it passed without the engine warning.
- pnpm recorded `minimumReleaseAgeExclude` entries for four revalidated exact releases while retaining supply-chain-policy checks for the lockfile.
- A workspace-local `.pnpm-store/` generated by the sandboxed tool path is ignored and is not repository content.

## Security evidence

- The Electron shell exists with the required isolation, sandbox, web-security, local-content, navigation, popup, CSP, preload, and IPC controls tested.
- Runtime smoke confirmed the only renderer bridge key is `getFoundationStatus`; Node globals are absent.
- CI grants only `contents: read`, does not persist checkout credentials, and pins third-party actions to immutable commits.
- The Node filesystem APIs in Checkpoint 2 are confined to the repository guard and its temporary architecture-test harness; no product package imports Node process APIs.
- Main uses Node path/URL APIs only to locate its own compiled local preload and renderer assets; these paths are not exposed to renderer code.
- No unrestricted filesystem, shell, environment, path, IPC, secret, media-execution, or AI-execution surface exists.
- `packages/export/src/index.ts` exports no symbol.
- Fixture binary and Git LFS budgets remain zero for M0.1.

## Deviations / limitations

- No product-scope deviation.
- Checkpoint 2 necessarily updated the root manifest and lockfile for the executable guard command, direct Node test types, and the declared timeline workspace edge; these are configuration support for the authorized code and do not introduce product functionality.
- The preload is emitted by Vite as a single CommonJS file so its Zod request validator remains available inside Electron's sandboxed preload environment; no new dependency was required.
- The earlier automated launch inspection was supplemented by the separately authorized manual checklist on 2026-07-22; the checklist passed from a fresh 100-file archive with real Electron visual, reload, bridge, IPC, and shutdown inspection.
- Hosted CI run `29882656691` passed every required step for the first reviewed commit `69cd74a122324734866708af7d1545fd01ce911c` after publication was separately authorized.
- Checkpoint 5 intentionally used the committed Checkpoint 4 source, so this documentary evidence commit is not part of the clean-room input it describes.
- The temporary clean-room and external hash/check files are outside the repository and may be removed later without changing committed evidence.
- The TypeScript alias package's internal compiler reports `6.0.3`; this is documented above and verified against the supported range.
- The first independent implementation judgment returned `FAIL`: README named `docs/fixture-policy.md` as inline code rather than linking it.
- Authorized commit `9cec1cfc5c8770bdec76f0bd7018f86494b5caa3` replaced that one line with a real relative Markdown link. Frozen install, format, lint, boundaries, typecheck, 64 tests, and build passed locally.
- Hosted CI run `29918638581` passed every required step for the corrected commit in 41 seconds.
- A new independent judge reproduced the full sequence in a pristine archive, inspected the real Electron runtime, passed all 16 criteria, and returned `PASS` without blocking findings.
- The documentary commit that records the re-judge verdict necessarily follows the reviewed commit and is not part of the clean-room input it describes.
- Manual smoke passed. The product owner explicitly approved the final evidence and froze M0.1 on 2026-07-22.

## Worktree after Checkpoint 5

All authorized Checkpoint 5 evidence/status updates are committed in one local commit. Its SHA and the clean-worktree check are reported externally to avoid commit-SHA self-reference.

`M0.1 FROZEN — PASS; M1.0 NOT AUTHORIZED`

## Manual smoke evidence

- Input: commit `09be5ceb8b7efa4e4fc04d573a71f0c5fff606ca` as an exact 100-file archive without Git metadata or dependencies.
- Authoritative directory: `/private/tmp/ai-video-editor-M0.1-manual-smoke-authoritative.DuKTCl`.
- Frozen install: PASS, 189 packages, `0.62s`; build: PASS.
- Visual result: nonblank neutral repository-foundation screen with no product controls.
- Reload/security: exact frozen bridge key `getFoundationStatus`; Node globals absent; real IPC status ready before and after reload.
- Close/termination: renderer target closed; no M0.1 product worker exists; all Electron processes and debugging listener were absent after app termination.
- Packaging: not applicable because packaging/distribution is an explicit M0.1 non-goal; the compiled development artifact passed.
- Binary evidence: screenshot remained temporary and untracked in compliance with the fixture policy; its SHA-256 is recorded in the checklist.
- Result: **PASS**. The smoke did not infer or perform the freeze; the product owner approved it later as a separate gate.

## Final freeze evidence

- Product-owner approval: explicit in the Codex task on 2026-07-22.
- Frozen implementation/evidence baseline: `76bfd5fdcc961902b6718a281d238ed8d9f8a49c`.
- Baseline hosted CI: run `29921045416` — PASS.
- Documentary freeze: recorded separately so the frozen baseline remains independently identifiable and the freeze commit does not self-reference.
- M1.0: not started and not authorized.
