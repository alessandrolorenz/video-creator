# M1.0 — Implementation Report

Status: CHECKPOINT 5 PASS — implemented and verified locally; Checkpoint 6 not authorized

## Frozen inputs

- Specification: `docs/specs/M1.0-transcript-selected-cut-spec.md`
- Specification freeze commit: `31810961e25da9e48c82ac6723b37797e28140a5`
- Normative Amendment 001: `docs/specs/M1.0-amendment-001-deterministic-ingest-results.md` — APPROVED 2026-07-22
- Normative Amendment 002: `docs/specs/M1.0-amendment-002-duration-and-capability-cache.md` — APPROVED 2026-07-22
- Corrected implementation plan: GATE 4 `PASS WITH NOTES` ON `88ef0d97b91c9e5ff3ed12f2f310d8a7424c3bfa`
- First plan-judge verdict: `FAIL` on plan commit `8391844426ab2f18aa8a990f2e88ca7d8ae79c14`
- Corrected-plan re-judge verdict: `FAIL` on commit `635af9be792c9f3231d56ee1447c8dc37b1106d0`
- Final plan-judge verdict: `PASS WITH NOTES` on commit `88ef0d97b91c9e5ff3ed12f2f310d8a7424c3bfa`; no blockers/scope creep

## Checkpoints

| Checkpoint | Authorization | Result | Commit |
|---|---|---|---|
| CP1 — IDs, transcript, guard | EXPLICITLY AUTHORIZED AND PUBLISHED | PASS — HOSTED CI GREEN | `20ac90c1f1727fda2f72c71f7ebd8cb752dbacd1` |
| CP2 — media metadata/parser | EXPLICITLY AUTHORIZED AND PUBLISHED | PASS — HOSTED CI GREEN | `af7d32e8e847af8fbb898eea2b7011ed82e3f9cd` |
| CP3 — utility worker | EXPLICITLY AUTHORIZED AND PUBLISHED AFTER CP2 CI GREEN | PASS — HOSTED CI GREEN | `fcd2c8b2bbd31f4bf8baba72e37a82d1881ea836` |
| CP4 — privileged main adapters/client/controller | EXPLICITLY AUTHORIZED AND PUBLISHED AFTER CP3 CI GREEN | PASS — HOSTED CI GREEN | `38879841b6dd7d0d0834261ae8ee38341b1b82fb` |
| CP5 — IPC/preload/lifecycle/real utility integration | EXPLICITLY AUTHORIZED AFTER CP4 CI GREEN | PASS | THIS LOCAL CHECKPOINT COMMIT |
| CP6 — renderer UX | NOT AUTHORIZED | PENDING | PENDING |
| CP7 — docs/clean-room | NOT AUTHORIZED | PENDING | PENDING |

## Verification evidence

### Checkpoint 1 authorization

The product owner explicitly authorized CP1:

> Autorizo o M1.0 Checkpoint 1 — pure IDs, transcript contract e guard foundation, com testes primeiro e commit local.

The product owner then explicitly authorized CP1 publication and conditionally authorized CP2 after green hosted CI:

> Autorizo a publicação do Checkpoint 1 e, após CI verde, o M1.0 Checkpoint 2 — pure media metadata e ffprobe output parser.

CP1 was pushed to `origin/main`. GitHub Actions run
[`29938830961`](https://github.com/alessandrolorenz/video-creator/actions/runs/29938830961)
passed on the exact CP1 commit `20ac90c1f1727fda2f72c71f7ebd8cb752dbacd1`, activating the CP2 authorization. CP2 publication, CP3 work, `ffprobe` installation, and external-tool execution were not authorized.

After local CP2 completion, the product owner explicitly authorized its publication and conditionally authorized CP3:

> autorização para publicar o CP2 e, somente após CI verde, iniciar o M1.0 Checkpoint 3 — dedicated media-probe utility worker.

CP2 was pushed to `origin/main`. GitHub Actions run
[`29942129632`](https://github.com/alessandrolorenz/video-creator/actions/runs/29942129632)
passed every required step on the exact CP2 commit `af7d32e8e847af8fbb898eea2b7011ed82e3f9cd`, activating the CP3 authorization.

After local CP3 completion, the product owner explicitly authorized its publication and conditionally authorized CP4:

> publicar o CP3 e, somente após CI verde, iniciar o Checkpoint 4 — main-owned privileged adapters, utility client e ingest controller.

CP3 was pushed to `origin/main`. GitHub Actions run
[`29944315413`](https://github.com/alessandrolorenz/video-creator/actions/runs/29944315413)
passed every required step on the exact CP3 commit `fcd2c8b2bbd31f4bf8baba72e37a82d1881ea836`, activating the CP4 authorization.

After local CP4 completion, the product owner explicitly authorized its publication and required a green hosted gate:

> autorizado para publicar o CP4 e aguardar o CI verde.

CP4 was pushed to `origin/main`. GitHub Actions run
[`29946730262`](https://github.com/alessandrolorenz/video-creator/actions/runs/29946730262)
passed every required step on the exact CP4 commit `38879841b6dd7d0d0834261ae8ee38341b1b82fb`. The product owner then authorized continuation, activating CP5. CP5 publication, CP6 work, and real external `ffprobe` installation/execution remain unauthorized.

### Checkpoint 1 implementation summary

- Added validated branded `AssetId`, `TranscriptDocumentId`, and `JobId` primitives with a 256-code-unit limit, closed immutable results, exact source preservation, and trusted throwing constructors.
- Added the framework-independent `@ai-video-assembly/transcript` workspace with only the declared `transcript → domain` runtime edge.
- Added strict V1 timed-transcript JSON parsing from already decoded text plus privileged typed context.
- Implemented the frozen precedence for encoding, JSON, schema, limits, entry shape/conversion, order, overlap, and active-asset duration bounds.
- Converted microseconds, milliseconds, and seconds exactly once with `Math.round`, rejected unsafe/invalid intervals, and stored only `TimeUs` safe integers.
- Produced deeply frozen canonical documents with privileged document/asset IDs, deterministic `entry-000001` IDs, preserved source text, covered range, and entry count.
- Evolved the repository guard to treat transcript as framework-independent, allow only its declared domain edge, reject Node/framework/process access, and restrict desktop Node/process access to exact `main`/`worker` runtime ownership.
- Replaced obsolete M0.1-only guard diagnostics with milestone-neutral repository-policy language.
- Added no registry dependency, binary fixture, LFS, media code, Electron ingest, IPC, renderer behavior, persistence, matching, timeline, render, export, or AI behavior.

### Checkpoint 2 implementation summary

- Added immutable media source, renderer-safe summary, primary-stream, rational, warning, media-specific probe-job, and closed result/error contracts.
- Added a pure parser for already-decoded `ffprobe` JSON text; it performs no process, filesystem, environment, executable, path, or Electron operation.
- Strictly validates the top-level object, stream array, format object, stream indices and types, attached-picture disposition, selected video/audio metadata, duration field types, and rotation containers.
- Deterministically sorts streams, excludes attached pictures, chooses the first eligible video and first audio, and preserves the specified missing-video-before-missing-audio precedence.
- Implements the exact Amendment 002 duration grammar, one `Math.round(seconds * 1_000_000)` conversion, safe positive `TimeUs`, format-first selection, greatest-selected-stream fallback, and terminal duration failure.
- Implements positive rational parsing with `BigInt`, GCD reduction, post-reduction safe-integer limits, optional invalid metadata, deterministic VFR/rate warnings, and frozen rotation selection/normalization.
- Returns warnings once in the fixed normative order and deeply freezes renderer-safe result structures.
- Allows only the planned runtime `media → domain` workspace edge while retaining the `ai-contracts → domain` type-only restriction and all pure-package security prohibitions.
- Adds only the local media-to-domain workspace link; no registry dependency, binary fixture, LFS content, media download, `ffprobe` package, Electron ingest, worker, IPC, renderer, persistence, timeline, render, export, or AI behavior was added.

### Checkpoint 3 implementation summary

- Added a dedicated NodeNext utility-worker compiler target emitted only under `apps/desktop/dist/worker` and integrated it into desktop typecheck/build scripts.
- Added a strict closed V1 privileged protocol for configuration, one media probe, exact-job cancellation, and shutdown; malformed versions, keys, nested shapes, IDs, executable strings, display names, sizes, and paths reject before spawn.
- Restricted configured executables to the exact bare name `ffprobe` or an already-resolved absolute path and required media paths to be absolute; the worker never reads environment or selects/changes an executable.
- Added an injectable bounded process executor using argument arrays, `shell: false`, ignored stdin, separate stdout/stderr pipes, exact byte caps, injected clock, `AbortSignal`, first-terminal latch, child termination, and safe spawn-code-only diagnostics.
- Added exact five-second/64-KiB `ffprobe -version` capability validation with fatal UTF-8, CRLF normalization, first-nonempty-line selection, the frozen 1–256-code-unit/control/regex rules, sanitized version result, and PASS-only per-runner exact-string cache.
- Added the fixed 30-second probe invocation with 8-MiB stdout and 1-MiB stderr caps, exact frozen arguments, terminal error mapping, fatal UTF-8 decoding, CP2 metadata parsing, and renderer-safe results.
- Added a one-active-job parent-port controller with configured/accepted/result messages, exact cancellation identity, runner invariant checks, unexpected-error containment, shutdown/disconnect abort, late-result suppression, and exactly-once cleanup.
- Added one owned Node spawn adapter and a compiled worker entrypoint; no main client, executable environment configuration, dialog/file adapter, IPC/preload surface, renderer behavior, real utility-process integration, real `ffprobe`, media fixture, registry dependency, persistence, timeline, render, export, or AI behavior was added.

### Checkpoint 4 implementation summary

- Added main-owned executable configuration with the exact absent-to-bare-`ffprobe` behavior, unchanged absolute override, fixed safe failures, and a single main-only runtime environment read.
- Added injectable native media/transcript file selectors with parented one-file dialogs, exact filters, cancellation before filesystem access, canonical regular-file validation, readability checks, media nonempty enforcement, and sanitized display names.
- Added a bounded transcript reader that checks the 20-MiB limit before and after reading, decodes fatal UTF-8, delegates to the frozen transcript parser, propagates cancellation, and suppresses late results.
- Added an injectable strict utility client that sends only the closed CP3 protocol, waits for configuration acceptance, validates complete renderer-safe responses, maps local worker failures to fixed errors, rejects stale/mismatched results, and settles every pending request during exit or shutdown.
- Added the in-memory ingest controller with every frozen state, one-active-operation semantics, exact branded IDs, stable-state preservation, retry behavior, media-replacement transcript invalidation, late-result rejection, and renderer-safe snapshots that exclude paths and transcript content.
- Added the planned local desktop-to-transcript workspace edge only. No IPC/preload/bootstrap registration, application lifecycle wiring, real Electron `utilityProcess` adapter, real `ffprobe` execution, persistence, timeline, render, export, or AI behavior was added.

### Checkpoint 5 implementation summary

- Added strict Zod V1 schemas for all five semantic ingest requests and every closed renderer-visible response, error code, media summary, transcript summary, and state-machine snapshot; malformed, extra, wrong-version, wrong-type, unsafe-message, hidden-path, and hidden-content values reject before work or exposure.
- Added five injectable main-process handlers with exact channel registration/removal, sender-owned parent-window resolution, safe immediate-failure mapping, strict output validation, and fixed path-redacted containment for unexpected exceptions.
- Extended the single frozen preload namespace with exactly `chooseMediaAsset`, `cancelMediaImport`, `chooseTimedTranscript`, `cancelTranscriptImport`, and `getIngestSnapshot`, retaining the foundation method and exposing no raw IPC/events/filesystem/process/command capability.
- Enforced the 250-ms minimum snapshot invocation interval in preload and validated every main response again before returning it to renderer.
- Added the real Electron `utilityProcess` transport with one resolved compiled worker path, no arguments, ignored stdio, a fixed service name, private child handles, removable listeners, and idempotent termination.
- Wired secure random opaque IDs, native dialogs, bounded transcript reading, executable configuration, probe client, controller, strict handlers, exact sender validation, window close, app shutdown, active-job cancellation, handler removal, and utility termination in the main bootstrap.
- Added a real Electron integration application that compiles and launches the actual worker through `utilityProcess` using only temporary text JavaScript executables/files; it covers compatible success, unsupported media, incompatible version, cancellation, redaction, client/worker shutdown, and no real `ffprobe`.
- Added conditional workspace exports so development/types continue to resolve source while compiled Electron runtime resolves `dist`; no dependency, lockfile, binary fixture, media download, renderer UX, persistence, timeline, render, export, or AI behavior was added.

### Test-first evidence

1. Added the domain ID tests, transcript contract/parser tests, and architecture negative cases before production implementations.
2. The intentional red run used `pnpm vitest run packages/domain/src/opaque-id.test.ts packages/transcript/src/timed-transcript.test.ts tests/architecture/check-boundaries.test.ts`.
3. The red result was three failed suites: both missing implementation modules and seven expected guard failures for transcript forbidden imports and spoofed privileged directories; the 25 pre-existing architecture cases still passed.
4. After minimal implementation and the workspace link, the targeted run passed 80 tests; additional process-global, exact-limit, total-text-boundary, and non-finite-token cases raised the final targeted result to 86 passing tests across three files.
5. The final complete suite passed 128 tests across 13 files, up from the frozen 64 tests across 11 files.

### Checkpoint 2 test-first evidence

1. Added the media contracts, captured MP4/MOV parser tests, and architecture edge tests before production implementation.
2. The intentional red run used the local Vitest binary for the two media test files plus the architecture guard.
3. The red result contained the expected missing media/domain workspace link and parser module plus the old runtime-edge rejection; 36 adjacent architecture tests remained green.
4. After the minimal contracts/parser/manifest/guard implementation and local workspace relink, the targeted suite passed 105 tests.
5. Added explicit regressions for format-first duration, attached-picture-only video, rational wrong-type versus unavailable-string classification, malformed rotation containers, and warning suppression; the final targeted result is 109 passing tests across three files.
6. The complete repository suite passes 203 tests across 15 files.

### Checkpoint 3 test-first evidence

1. Added protocol, bounded-process, capability/probe runner, worker-controller, build-target, and worker-security tests before production modules/configuration.
2. The intentional red run contained four missing implementation-module suites and one missing `tsconfig.worker.json` build-target failure.
3. After minimal implementation and workspace relinking, the targeted suite exposed and fixed late child `error` handling after a terminal timeout; the remaining runner failures were corrected by making the test harness respect the cache lookup microtask boundary.
4. The final targeted worker suite passes 70 tests across six files.
5. Coverage includes exact spawn options/arguments/timeouts/caps, at-cap success, cap breach, sync/async spawn errors, signals, nonzero exits, fatal UTF-8, invalid JSON, all capability-line classes, cancellation/timeout/output races, late events, result redaction, all Amendment 002 cache behaviors, strict protocol rejection-before-work, one active job, exact cancellation, shutdown, disconnect, and late-result suppression.
6. The complete repository suite passes 273 tests across 21 files.

### Checkpoint 4 test-first evidence

1. Added executable-configuration, privileged file-import, transcript-reader, strict utility-client, ingest-controller, and main-security tests before production modules.
2. The intentional red run contained five missing implementation-module suites plus the missing local desktop-to-transcript workspace link.
3. The first green iteration corrected one test-boundary assumption: canonical source paths are intentionally retained inside privileged main adapters, while the controller snapshot is the enforced renderer-safe boundary.
4. Focused tests cover dialog parenting/filters/cancellation, validation precedence, pre-read/post-read bounds, fatal UTF-8, configuration exactness, no utility creation after invalid configuration, strict message validation, crash/exit/shutdown cleanup, all controller states, exact IDs, retries, stable preservation, replacement invalidation, stale-result rejection, and path/content redaction.
5. The final targeted CP4 suite passes 54 tests across six files.
6. The complete repository suite passes 327 tests across 27 files.

### Checkpoint 5 test-first evidence

1. Added strict shared IPC, main handler, preload, utility adapter, lifecycle, Electron integration, and security tests before their production modules/wiring.
2. The intentional red run contained four missing-module suites, four missing bridge behaviors, two expected security failures, and the missing real integration application; three adjacent legacy security tests remained green.
3. The first implementation iteration passed 43 non-integration CP5 tests and exposed the compiled-runtime workspace export mismatch only when the real Electron harness ran.
4. Conditional source/type/default exports fixed that runtime-only mismatch without changing dependency resolution. A second real-harness iteration identified an Electron ESM top-level `app.whenReady()` deadlock; converting the harness into an explicit Electron application and scheduling work from the ready callback removed it.
5. The real integration passes using the compiled worker and temporary text-only executables/files for compatible success, numeric failure, incompatible capability, cancellation, redaction, and shutdown.
6. With ignored domain/media/transcript `dist` directories temporarily absent, all 370 non-GUI tests still pass, proving development resolution does not depend on stale generated artifacts; the directories were then restored.
7. The complete repository suite passes 371 tests across 33 files, including the real Electron/utility-process integration.

### Authoritative command sequence

| Command | Result |
|---|---|
| `env CI=true pnpm install --frozen-lockfile` | PASS — all 8 workspaces, lockfile already up to date |
| `env CI=true pnpm format:check` | PASS |
| `env CI=true pnpm lint` | PASS — zero warnings |
| `env CI=true pnpm check:boundaries` | PASS |
| `env CI=true pnpm typecheck` | PASS — root plus 7 participating workspaces |
| `env CI=true pnpm test` | PASS — 13 files / 128 tests |
| `env CI=true pnpm build` | PASS — transcript and all prior build targets |

`CI=true` supplies pnpm's documented non-interactive behavior in the Codex PTY-less execution environment; it does not alter application behavior or dependency resolution.

The same full authoritative sequence is rerun for CP2 after all code and evidence edits; its final results are recorded below before the checkpoint commit.

| CP2 command | Result |
|---|---|
| `env CI=true pnpm install --frozen-lockfile` | PASS — all 8 workspaces, already up to date |
| `env CI=true pnpm format:check` | PASS |
| `env CI=true pnpm lint` | PASS — zero warnings |
| `env CI=true pnpm check:boundaries` | PASS |
| `env CI=true pnpm typecheck` | PASS — root plus 7 participating workspaces |
| `env CI=true pnpm test` | PASS — 15 files / 203 tests |
| `env CI=true pnpm build` | PASS — media and all prior build targets |

The same authoritative sequence is rerun for CP3 after all code and evidence edits; its final results are recorded below before the checkpoint commit.

| CP3 command | Result |
|---|---|
| `env CI=true pnpm install --frozen-lockfile` | PASS — all 8 workspaces, exact locked graph |
| `env CI=true pnpm format:check` | PASS |
| `env CI=true pnpm lint` | PASS — zero warnings |
| `env CI=true pnpm check:boundaries` | PASS |
| `env CI=true pnpm typecheck` | PASS — root plus 7 participating workspaces and dedicated worker target |
| `env CI=true pnpm test` | PASS — 21 files / 273 tests |
| `env CI=true pnpm build` | PASS — dedicated worker artifact and all prior build targets |

The same authoritative sequence is rerun for CP4 after all code and evidence edits; its final results are recorded below before the checkpoint commit.

| CP4 command | Result |
|---|---|
| `env CI=true pnpm install --frozen-lockfile` | PASS — all 8 workspaces, exact locked graph |
| `env CI=true pnpm format:check` | PASS |
| `env CI=true pnpm lint` | PASS — zero warnings |
| `env CI=true pnpm check:boundaries` | PASS |
| `env CI=true pnpm typecheck` | PASS — root plus 7 participating workspaces and dedicated worker target |
| `env CI=true pnpm test` | PASS — 27 files / 327 tests |
| `env CI=true pnpm build` | PASS — main adapters/client/controller, dedicated worker artifact, and all prior build targets |

The same authoritative sequence is rerun for CP5 after all code and evidence edits; its final results are recorded below before the checkpoint commit.

| CP5 command | Result |
|---|---|
| `env CI=true pnpm install --frozen-lockfile` | PASS — all 8 workspaces, exact locked graph |
| `env CI=true pnpm format:check` | PASS |
| `env CI=true pnpm lint` | PASS — zero warnings |
| `env CI=true pnpm check:boundaries` | PASS |
| `env CI=true pnpm typecheck` | PASS — root plus 7 participating workspaces and dedicated worker target |
| `env CI=true pnpm test` | PASS — 33 files / 371 tests, including real Electron `utilityProcess` integration |
| `env CI=true pnpm build` | PASS — strict IPC/preload, lifecycle, compiled main/worker, and all prior targets |

## Security and scope evidence

- `rg` found no Node, Electron, React, or `process` use in domain/transcript sources.
- The executable guard passed in all 34 architecture tests, including the real repository plus temporary positive and negative repositories.
- Negative cases cover undeclared transcript edges, forbidden Node/Electron/React/process access, `main-spoof`, `worker-copy`, preload/renderer Node imports, fixtures, disguised binary content, LFS, undeclared edges, and the empty export package.
- Exact `apps/desktop/src/main` and future exact `apps/desktop/src/worker` remain the only non-test desktop runtime ownership allowed to use Node/process APIs.
- The lockfile diff contains only the local `packages/transcript → packages/domain` link; no registry package or version changed.
- `packages/transcript/dist` and its workspace `node_modules` link are ignored generated state.
- `ffprobe` remains absent and untouched.

For CP2, the renderer-safe serialization test proves the result contains no absolute path, raw output, stderr, executable, arguments, environment, PID, or captured probe payload. The parser imports only `@ai-video-assembly/domain` and local media contracts; executable guard tests preserve every Node/Electron/framework/process prohibition.

The final inventory found no Node, Electron, React, or process-global use in media runtime sources. The only textual `process` match in the package is the negative security-test description. The lockfile diff is exactly one local `packages/media → packages/domain` link.

For CP3, source and compiled-artifact inspection found `node:child_process` only in the owned spawn adapter, `process.parentPort` only in the worker entrypoint, and `shell: false` in the bounded executor. No compiled worker JavaScript contains `process.env`, `AI_VIDEO_ASSEMBLY_FFPROBE_PATH`, renderer, or preload references. Worker responses and error tests prove no executable, absolute path, raw stdout/stderr, command arguments, environment, PID, or localized process error crosses the boundary.

For CP4, runtime source inspection finds the executable environment key only in the main-owned configuration module; the worker, preload, and renderer never read it. Canonical paths and transcript source text remain confined to privileged main-owned objects, while snapshot tests prove no path, transcript entries, or transcript content crosses the controller boundary. Bootstrap, preload, and renderer do not import the CP4 modules, so the new services remain deliberately unwired. Compiled main artifacts preserve this ownership, and the lockfile change is exactly the planned local desktop-to-transcript workspace link.

For CP5, source/declaration tests prove the shared, preload, and renderer surfaces contain no absolute path, environment, process/utility handle, stdout/stderr, PID, filesystem handle, or generic command/event primitive. All request and response schemas are strict and closed. Main validates the exact sending window, and lifecycle tests prove active cancellation, handler removal, pending settlement, and utility termination occur once. The real integration captures only a sanitized result marker and asserts that neither the temporary root nor privileged field names appear. Worker stdio is ignored; no raw child handle crosses its adapter. The integration uses no real `ffprobe`, binary media, network, shell, or registry dependency, and the lockfile is unchanged.

## Deviations, failures, and retries

- No product/spec deviation.
- The initial offline frozen install was attempted without a TTY after pnpm determined that `node_modules` needed reconstruction; pnpm correctly aborted rather than removing it interactively.
- A non-interactive offline retry exposed an incomplete sandbox-visible package cache and registry `ENOTFOUND` warnings. The approved frozen network retry restored exactly the 189 already-locked packages, reported 189 reused / 0 downloaded, and made no lockfile change.
- Until that restoration, `pnpm vitest`/`pnpm exec vitest` also encountered pnpm's dependency-status reinstall guard. The explicit local Vitest binary was used only for targeted development feedback; the authoritative full suite used the required root pnpm script after the frozen install passed.
- Prettier initially reported five new/changed source files; formatting was applied mechanically and the authoritative check passed.

During CP2, the first frozen offline relink recreated `node_modules` but the package-manager supply-chain verifier attempted registry checks unavailable in the sandbox. The approved frozen retry restored exactly the locked graph with 189 reused packages, zero downloads, no version change, and no lockfile registry change. The direct local TypeScript/Vitest binaries were used only for targeted feedback; final evidence uses the authoritative root scripts.

During CP3, adding the two local desktop workspace edges again required pnpm to reconstruct links. The sandboxed frozen attempt could not reach supply-chain verification endpoints after recreating `node_modules`; the approved frozen retry restored the same 189 locked packages with 189 reused, zero downloaded, and no registry version change. A first TypeScript pass found only missing contextual parameter annotations in the owned spawn adapter; explicit types fixed it without behavioral change. A first lint pass found three mechanical const/unused-parameter issues; they were corrected without disabling rules.

During CP4, the local desktop-to-transcript edge was installed from the existing workspace without adding a registry package. The Electron dialog import was made lazy so pure unit tests do not load the Electron runtime. Typecheck findings were confined to fake-adapter typings and one display-name fixture and were corrected without behavioral or specification change. Utility-originated diagnostic text is normalized to fixed local errors before it can reach the controller boundary.

During CP5, the first `pnpm vitest` targeted invocation encountered pnpm's non-interactive module-status guard, so the already-installed local Vitest binary recorded the intentional red state; final evidence uses the required root script. The first typecheck found only widened literals in test fakes and branded test-call arguments; they were corrected without production change. Real Electron execution exposed that source-only workspace exports were not executable from compiled NodeNext output, so the three owned runtime workspaces received `development`/`types` source conditions and a compiled default condition. Initial harness attempts also exposed ambiguous standalone-module launch behavior and an Electron ESM top-level-ready deadlock. An explicit application directory plus ready-callback scheduling fixed both. Sandbox GUI execution first required explicit elevation; after the owner reconfirmed authorization, the corrected harness and complete suite passed. No product/spec deviation resulted.

## Worktree and generated state

- Baseline before CP5: clean synchronized `main` / `origin/main` at published CP4 commit `38879841b6dd7d0d0834261ae8ee38341b1b82fb`.
- Tracked changes are limited to strict IPC/handler/preload/lifecycle/utility integration modules and tests, main bootstrap wiring, three conditional runtime exports required by compiled Electron, and evidence files.
- Ignored generated state consists of dependencies/workspace links and build `dist` outputs; no tracked generated file exists.
- `git diff --check`: PASS.
- Local CP5 commit: this report is part of the single checkpoint commit; its exact SHA is recorded in the handoff after creation.
- CP5 remote publication and hosted CI are not authorized.

`M1.0 CHECKPOINT 5: PASS — SINGLE LOCAL COMMIT; CHECKPOINT 6 NOT AUTHORIZED`
